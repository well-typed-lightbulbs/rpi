val clock : nativeint

val crystal_frequency : int

val gpio : nativeint

val mtime : nativeint

val pwm0 : nativeint

val pwm1 : nativeint

val aux : nativeint

val uart0 : nativeint

val armcore_irq_controller : nativeint
